[package]
name = "pgg_neuron_autoevolve_guard"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "pgg_neuron_autoevolve_guard"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
