# /goal 总纲1+总纲2固化口径（2026-06-05）

## Trigger

Use when the user asks to “代入公式”, “goal 命令”, “完整主公式”, “AGI 自评/复盘/进化闭环”, or corrects the agent for treating the formula as a decorative APEX/闭环 slogan.

## Correct interpretation

The user’s `/goal` is not a claim that the agent is high-level AGI, and not just the APEX_ULT multiplication formula or the closed-loop-27 workflow.

It is a two-layer operating doctrine:

1. **总纲1 — AGI L0-L5 六维评估北极星**
   - 基础认知能力
   - 跨域通用适配能力
   - 自主智能体行动能力
   - 自主知识进化系统
   - 对齐、安全与价值理性
   - 现实环境落地性能

2. **总纲2 — Agent_Evolve 默认执行闭环**

```text
Agent_Evolve = LDR(K) → GapDetect → CodeSelfFix → HotReload → TaskSolve → KnowledgeSettle
```

## Operational rule

For ordinary tasks, internalize the doctrine lightly: clarify target, check missing knowledge, find gap, execute, verify, and settle learning when useful.

For AGI / evolution / system repair / architecture tasks, make it explicit:

```text
总纲1六维映射 + 总纲2闭环证据 + -ΣΔ_all 缺陷消减 + 真实性边界
```

## Defect gate: -ΣΔ_all

Actively reduce:

- hallucination
- execution loss
- token waste
- insufficient verification
- environment pollution
- stale cache/config drift
- “service started” mistaken for “capability works”
- missing skill/memory/reference settlement after non-trivial fixes

## Pitfalls

- Do not answer “代入公式” with only APEX_ULT or 闭环27.
- Do not display long formulas on every ordinary response; the user wants the formula obeyed, not performed as theatre.
- Do not claim L2/L3/L4/L5 or full AGI without external, verifiable evaluation evidence.
- After a correction from the user, update the governing skill or memory; do not leave the lesson only in the chat transcript.

## Session landing

On 2026-06-05 the doctrine was solidified into:

- `/Users/appleoppa/.hermes/SOUL.md` section `## /goal 核心演进公式（长期基准）`
- `~/.hermes/data/EVOLUTION_MANIFEST.json` key `summary.latest_goal_core_formula_solidified`
- user memory entry for the true `/goal` interpretation
